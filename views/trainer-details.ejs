<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pokemon Management</title>
</head>
<body>
    <p>Name: <%= trainer.name %></p>
    <p>Pokemons: </p>
    <ul>
        <% pokemons.map((pokemon)=> { %>
            <li>
                <p>
                    <%= pokemon.name %>
                </p>
            </li>
            <% }) %>
    </ul>
    <form method="POST" action="/delete/trainer/<%= trainer.trainer_id %>">
        <button type="submit" id="deleteItemBtn">Delete Trainer</button>
    </form>
    <a href="/">Go Back</a>
</body>
</html>